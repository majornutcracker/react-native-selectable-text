import * as React from "react";

import { SelectableTextViewProps } from "./types";

export default function SelectableTextView(props: SelectableTextViewProps) {
  return <iframe style={{ flex: 1 }} />;
}
